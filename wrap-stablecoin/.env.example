# Copy to .env and adjust paths as needed.
# Used by `anchor run local` (scripts/run_local.sh).

# Wallet that pays bootstrap txs and seeds the vault as authority/admin.
ANCHOR_WALLET_PATH=fixtures/user/wallet.json

# solana-test-validator RPC port (default matches run_backend_smoke.sh).
RPC_PORT=8901

# Optional overrides:
# LEDGER_DIR=/tmp/wrap-stablecoin-ledger
# LOG_DIR=/tmp/localnet-logs
# FORCE_KLEND_FETCH=1

# --- OS ---
.DS_Store
**/.DS_Store
Thumbs.db

# --- IDE / editors ---
.idea/
*.swp
*.swo
*~

# --- Environment & secrets ---
.env
.env.*
**/.env
**/.env.*
**/.env.local
**/.env.local.*
!.env.example
!**/.env.example
**/.secrets/
*-keypair.json
**/*-keypair.json
*.pem
*.key
credentials.json
**/credentials*.json

# --- Node (any workspace / app) ---
node_modules/
**/node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# --- Next.js / frontends ---
.next/
out/
.turbo/
*.tsbuildinfo

# --- Anchor / Solana / Rust ---
.anchor/
**/target/
target/
so/
test-ledger/
**/*.rs.bk
*.so

# --- Yarn (Berry) ---
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# --- Coverage / caches ---
coverage/
.nyc_output/
.eslintcache

# --- Misc logs ---
*.log

# --- Per-cluster deploy artifacts (regenerated by `cli init`) ---
# Hosted clusters are untracked too: carry deployments/<network>.json between
# machines out of band, or re-run `pnpm cli init` against the deployed vault
# (idempotent: every step is skipped when its account already exists).
deployments/

wrap-stablecoin/scripts/devnet-e2e/borrower-*.json

.secrets/
